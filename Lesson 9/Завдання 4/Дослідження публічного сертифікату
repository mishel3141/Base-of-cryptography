Ланцюжок сертифіката для домену robotdreams.cc складається з трьох сертифікатів та кінцевої частни.

1.  Сертифікат виданий для домену robotdreams.cc:
   - Іssuer (видав): Google Trust Services (CN = WE1).
   - Ключ: використовується ecPublicKey з розміром 256 біт.
   - Алгоритм підпису: ecdsa-with-SHA256 (ECDSA з SHA256).
   - Термін дії: NotBefore: Oct 8 05:05:37 2024 GMT, NotAfter: Jan 6 05:05:36 2025 GMT.

 2. Сертифікат виданий Google Trust Services (CN = WE1):
   - Іssuer (надав): Google Trust Services LLC (CN = GTS Root R4).   
   - Ключ: використовується ecPublicKey з розміром 256 біт.
   - Алгоритм підпису: ecdsa-with-SHA384 (ECDSA з SHA384).
   - Термін дії: NotBefore: Dec 13 09:00:00 2023 GMT; NotAfter: Feb 20 14:00:00 2029 GMT

 3. Корінний сертифікат Google Trust Services LLC (CN = GTS Root R4):
   - Issuer: GlobalSign nv-sa (CN = GlobalSign Root CA).
   - Ключ: використовується ecPublicKey з розміром 384 біт.
   - Алгоритм підпису: RSA-SHA256.
   - Термін дії: NotBefore: Nov 15 03:43:21 2023 GMT; NotAfter: Jan 28 00:00:42 2028 GMT   

У ланцюжку найдовший термін дії має сертифікат, виданий Google Trust Services (WE1) до 6 січня 2029.

У сертифікаті для robotdreams.cc використовується ключ з еліптичною кривою (ECP) з розміром 256 біт. 
Сертифікат підписаний з використанням ECDSA (Elliptic Curve Digital Signature Algorithm) і алгоритму хешування SHA-256.
Однак точна еліптична крива не вказана в сертифікаті. Зазвичай для ключів розміру 256 біт для ECDSA 
використовується популярна еліптична крива NIST P-256 (або secp256r1). Ця крива є стандартом для цифрових підписів 
в багатьох криптографічних системах і сертифікатах SSL/TLS. Таким чином, хоча точна кривизна не вказана, можна 
з великою ймовірністю припустити, що для цього сертифіката використано P-256 або іншу сумісну криву з розміром 256 біт.

Останій фрагмент ланцюжка є частиною звіту SSL/TLS з'єднання і надає детальну інформацію про сертифікат 
та процес перевірки безпеки при встановленні зашифрованого з'єднання між клієнтом і сервером. Ось основні моменти:

Сертифікат сервера:
- Суб'єкт (subject): CN = robotdreams.cc — це означає, що сертифікат видано для домену robotdreams.cc.
- Виданий (issuer): C = US, O = Google Trust Services, CN = WE1 — сертифікат виданий центром сертифікації 
   Google Trust Services, що є організацією в США.

Алгоритм підпису:
- Peer signing digest: SHA256 — для підпису використовується хеш-функція SHA-256.
- Peer signature type: ECDSA — використано алгоритм підпису ECDSA (Elliptic Curve Digital Signature Algorithm), 
  який є безпечним і використовує еліптичні криві для генерації підписів.

Ключ сервера:
- Server Temp Key: X25519, 253 bits — для встановлення тимчасового ключа для обміну використовується алгоритм X25519, 
  який є сучасним і безпечним методом для обміну секретами, що працює на 253 бітах.

SSL/TLS з'єднання:
- SSL handshake has read 2832 bytes and written 396 bytes — це означає, що під час процесу встановлення з'єднання 
  було зчитано 2832 байти даних та записано 396 байтів.
- TLSv1.3, Cipher: TLS_AES_256_GCM_SHA384 — з'єднання використовує TLS 1.3 і шифрування TLS_AES_256_GCM_SHA384. 
  Це дуже сильне шифрування з 256-бітним ключем, яке забезпечує конфіденційність і цілісність переданих даних.
- Server public key is 256 bit — публічний ключ сервера має довжину 256 біт, що є оптимальним для сучасних протоколів.
- Secure Renegotiation IS NOT supported — сервер не підтримує безпечну реконфігурацію з'єднання (що може бути важливо 
  для деяких старих або спеціальних використань).
- Compression: NONE, Expansion: NONE — не використовується стиснення чи розширення даних у процесі з'єднання.

Код повернення перевірки:
- Verify return code: 0 (ok) — це вказує на те, що перевірка сертифіката була успішною і немає жодних проблем 
  з його дійсністю.

Підсумок:
- Цей звіт показує, що під час SSL/TLS з'єднання з сервером robotdreams.cc, сертифікат сервера було перевірено, 
  і з'єднання було успішно встановлено з використанням сучасних алгоритмів шифрування та підпису, таких як ECDSA 
  та TLS_AES_256_GCM_SHA384. Вся інформація, зібрана під час встановлення з'єднання, підтверджує його безпеку та 
  дійсність сертифікатів.
